name: Deploy to App to stg EB Environment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults: 
      run: 
        working-directory: app

    permissions:
      id-token: write
      contents: read

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.100'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release

    - name: Publish
      run: dotnet publish --configuration Release --output ./publish

    - name: Configure AWS credentials
      id: configure-aws-credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: arn:aws:iam::767397701747:role/GithubActions
        role-session-name: github-actions
        aws-region: us-east-1

    - name: Zip published application
      run: zip -r app.zip ./publish

    - name: Upload to S3
      run: aws s3 cp app.zip s3://isaac-beanstalk-stg-bucket/app.zip

    - name: Create new application version
      run: aws elasticbeanstalk create-application-version --application-name isaac-beanstalk-stg-DotNetApp --version-label --version-label v1.0.1 --source-bundle S3Bucket=isaac-beanstalk-stg-bucket,S3Key=app.zip

    - name: Update Elastic Beanstalk environment
      run: aws elasticbeanstalk update-environment --environment-name isaac-beanstalk-stg-DotNetEnvironment --version-label --version-label v1.0.1